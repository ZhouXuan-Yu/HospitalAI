# 医院 AI 药学系统 MVP 产品需求文档

文档版本：V0.3  
更新时间：2026-08-03  
文档状态：MVP 产品、技术、UI 和验收基线已冻结，可进入开发  
首个试点：呼吸内科住院患者的社区获得性肺炎（可根据合作医院调整）

## 1. 文档目的

本文档将当前已经确认的需求整理为第一版产品基线，用于医院需求确认、产品原型设计、技术架构设计、开发拆分和验收。

本产品是临床用药辅助系统，不是自动诊断或自动开方系统。正式处方和医学结论必须由具备相应权限的专业人员审核确认。

## 2. 产品背景

大型医院的患者用药信息分散在 HIS、EMR、LIS、医嘱、药品目录、指南、说明书和历史就诊记录中，带来以下问题：

- 医生开药前汇总患者信息耗时。
- 历史过敏、不良反应或无效用药可能没有及时影响本次处方。
- 指南、说明书、院内规则和医生经验缺少统一证据链。
- 不合适的处方缺少及时、明确、可解释的提醒。
- 用药、调整原因、疗效和结局没有形成连续的患者药学档案。
- 临床数据难以转化为口径明确、可追溯的科研数据。
- 未经审核的个案经验可能被误当成普遍规律。

## 3. 产品定位

> 面向医生、药师和科研人员的院内部署临床用药决策与科研数据工作台。

产品包含两个相互连接的核心能力：

1. 处方推荐与处方审核。
2. 患者长期追踪与科研数据沉淀。

完整闭环如下：

```text
患者上下文汇总
  -> 处方前候选方案推荐
  -> 医生审核、修改或驳回
  -> 处方后安全审核
  -> 真实用药和疗效反馈
  -> 出院及再次入院追踪
  -> 科研数据集和报告草稿
  -> 专业人员审核
  -> 审核通过的知识结论
  -> 反哺后续处方推荐
```

## 4. 产品目标

### 4.1 处方决策效率和安全

- 自动汇总医生开药所需的患者信息。
- 在开药前提供多个有依据的候选方案。
- 解释为什么推荐、为什么排除以及与同类药物的差异。
- 在处方提交前后发现过敏、禁忌、相互作用和剂量等风险。
- 保留医生最终决定和修改原因。

### 4.2 患者纵向用药档案

- 串联患者多次门诊、住院和用药过程。
- 记录实际用药、方案调整、疗效、不良反应和出院结局。
- 将已确认的历史风险带入下一次就诊。
- 防止再次推荐已经确认过敏或发生严重不良反应的药物。

### 4.3 科研数据资产

- 将真实诊疗事件沉淀为结构化、可追溯的数据。
- 支持研究队列、变量口径、统计结果和报告草稿生成。
- 区分事实数据、计算结果、AI 草稿和审核后结论。
- 只允许经过审核的结论进入正式知识库。

## 5. 首个试点范围

### 5.1 科室与病种

- 试点科室：呼吸内科。
- 试点病种：住院患者的社区获得性肺炎。
- 调整原则：合作医院如有更合适的数据和专家资源，可以替换科室和病种，但保留相同业务闭环。

### 5.2 MVP 数据范围

- 第一开发阶段不连接真实 HIS，使用标准模拟数据导入和 HIS 模拟器。
- 20 例以上模拟或脱敏历史病例作为基本验证集。
- 至少 5 例跨就诊周期案例。
- 包含已确认药物过敏、不良反应、换药或停药案例。
- 一份可用于试点的院内药品目录。
- 经过医院确认的指南、药品说明书、临床路径及院内规则。
- 模拟器覆盖正常、缺失、延迟、冲突、重复、数据更新和接口失败场景。

以上病例数量只用于验证产品闭环，不代表足以支撑正式医学研究结论。正式研究样本量需根据研究方案确定。

## 6. 核心用户

### 6.1 临床医生

- 查看患者上下文、候选方案及证据。
- 采纳、修改或驳回推荐。
- 处理处方安全提醒并填写必要理由。
- 确认用药反馈、过敏和不良反应事件。

### 6.2 临床药师或审方药师

- 维护或审核药学规则。
- 复核高风险处方提醒。
- 审核过敏、不良反应和用药经验。
- 参与知识结论审核。

### 6.3 科研负责人

- 定义研究问题、人群和观察指标。
- 设置纳入排除标准和变量口径。
- 检查数据质量。
- 审核统计结果、报告草稿和知识结论。

### 6.4 医院信息科和系统管理员

- 管理角色、权限和审计。
- 配置医院接口和数据同步。
- 维护院内药品目录和基础字典。
- 管理数据脱敏、备份和系统运行状态。

### 6.5 超级管理员

- 用于研发调试、系统集成验证和医院运维排障。
- 可以访问医生、药师、科研、知识审核和系统管理的全部页面。
- 可以执行系统中已经定义的全部功能权限。
- 所有查看、修改、导出、模拟和权限切换操作必须记录审计日志。
- 超级管理员不能绕过医疗硬规则、伪造审核人或删除审计记录。
- 开发和测试环境可以长期启用超级管理员。
- 医院生产环境默认禁用超级管理员，需要调试时通过双重认证、指定内网地址、填写启用原因并限时开启。
- 超级管理员授权到期后自动失效，启用、使用和失效过程均进入独立审计记录。

## 7. 处方推荐原则

### 7.1 推荐依据优先级

1. 已确认过敏、严重不良反应和明确禁忌。
2. 患者当前诊断、病情和检验检查指标。
3. 经过医院确认的权威指南和药品说明书。
4. 本院及本科室认可的临床路径和用药规范。
5. 患者既往有效、无效、停药和换药记录。
6. 科室常用药品。
7. 医生个人用药习惯。

医生习惯和科室常用药只能在安全性和适用性基本相同的候选药物之间影响排序。

### 7.2 冲突处理

当医生常用药与患者安全信息或权威依据冲突时：

- 优先执行患者安全信息和经过确认的医学规则。
- 不把存在明确硬性冲突的药物作为正常候选方案。
- 告知医生该药不适合当前患者的具体原因。
- 展示命中的患者信息、规则和证据来源。
- 对需要专业判断的非硬性冲突进行醒目提醒，由医生作最终决定。

### 7.3 推荐解释

每个候选方案至少展示：

- 推荐药品和方案摘要。
- 适合当前患者的原因。
- 使用了哪些患者上下文信息。
- 对应的指南、说明书或院内规则。
- 与主要同类药物的差异。
- 主要风险及需要监测的指标。
- 被排除药物及排除原因。
- 可能影响推荐的缺失信息。
- 证据名称、版本、生效时间和定位信息。

## 8. 核心功能需求

### 8.1 患者上下文汇总

系统应汇总：

- 基本信息和当前就诊信息。
- 当前诊断、既往疾病和症状摘要。
- 检查检验结果及变化趋势。
- 肝肾功能等影响用药的关键指标。
- 当前医嘱、处方和实际用药。
- 既往用药及其疗效反馈。
- 药物过敏和不良反应。
- 出院带药及再次入院关联信息。

所有数据必须标记来源、采集时间和缺失状态。AI 摘要不能覆盖原始记录，医生应能返回查看原文。

患者与科室数据采用以下归属规则：

- 同一名患者在院内只有一个 `PatientProfile` 患者主档。
- 每次挂号、急诊或住院分别形成 `Encounter` 就诊记录，并关联同一个患者主档。
- 同一次住院中的转科、会诊和多学科协作记录为科室参与关系或下级就诊事件，不重复创建患者。
- 科室诊断、医嘱和病历保留原科室、原就诊、时间和数据来源，不能合并成无法追溯的总文本。
- 处方推荐读取当前就诊上下文，同时加载全院范围内与用药安全相关的患者摘要。
- 跨科室安全摘要至少包含确认过敏、严重不良反应、当前用药、近期停换药和明确影响用药的关键问题。
- 其他科室详细病历继续按照医院角色、科室和敏感信息权限开放。

### 8.2 处方前候选方案推荐

- 生成 2 至 3 个候选方案。
- 只从当前医院可用药品目录中选择正常候选药物。
- 推荐前执行过敏、禁忌、相互作用和重复用药检查。
- 根据患者特征和已确认规则进行剂量计算或调整。
- 信息缺失、证据冲突或判断不确定时主动提示。
- 支持查看详细证据、同类药比较和排除原因。
- 支持医生采纳、修改或驳回。

剂量、频次上限等确定性计算必须由规则或经过验证的计算函数完成，不能让大模型自由生成关键数值。

### 8.3 处方后安全审核

系统检查：

- 已确认过敏和历史严重不良反应。
- 禁忌证及慎用情况。
- 重复用药和药物相互作用。
- 剂量、频次、给药途径和疗程。
- 特殊人群及肝肾功能调整。
- 处方与诊断或病情的匹配关系。
- 必要监测项目是否缺失。

风险建议分为：

- 阻断：医院规则规定不得继续的已确认风险。
- 强提醒：必须处理或说明理由的较高风险。
- 一般提示：供医生参考，不影响处方流程。

风险等级和处置权限最终由合作医院确认并配置。

当患者同时存在多个科室的有效处方或医嘱时：

- 新处方提交前检查跨科室重复用药、相互作用和治疗方案冲突。
- 冲突信息展示涉及科室、药品、医生和医嘱时间。
- 系统不得自动停止或取消其他科室已经生效的医嘱。
- 阻断风险创建跨科室协同任务，处理完成前不能正常提交。
- 强提醒允许医生说明理由后继续，并通知原开方医生和药师。
- 药师通过统一冲突队列查看双方方案、沟通记录和处理结果。
- 停用、替换或继续使用原方案均记录责任人和原因。
- 紧急场景的先用药后补审权限由合作医院配置。

### 8.4 医生决策记录

- 保存系统原始推荐版本。
- 记录医生采纳、修改或驳回结果。
- 展示修改前后的方案差异。
- 保存原因标签和补充说明。
- 保存风险提醒处理结果。
- 保存操作人、时间和审计信息。

医生行为记录不能未经审核直接转化为医学规则。

### 8.5 长期用药追踪

采用“用药事件链作为底座 + 专病模板 + 通用模板兜底”的方案，以“患者 + 就诊 + 用药事件”形成时间线：

- 开药原因和对应诊断。
- 计划用药与实际用药。
- 开始、调整、暂停和停止时间。
- 剂量和给药方式变化。
- 症状、检验和检查结果变化。
- 疗效评价及其依据。
- 疑似或确认的不良反应。
- 换药、停药和继续用药原因。
- 出院结局和出院带药。
- 复诊、再入院及历史风险继承。

专病模板中的疗效指标、观察时间和判定标准必须由医生、药师和科研人员确认。

### 8.6 历史风险继承

患者再次就诊时自动加载：

- 已确认药物过敏。
- 已确认严重不良反应。
- 历史无效或因风险停用的方案。
- 历史有效方案及当时适用条件。
- 上次出院诊断和出院带药。

已确认过敏和医院定义的严重不良反应必须进入确定性安全规则，不能只作为大模型提示文本。

当患者同时存在多个进行中的科室就诊时，系统还必须加载其他科室当前有效的处方、医嘱和实际用药，用于重复用药、相互作用和跨科室方案冲突检查。

### 8.7 科研队列与数据集

- 按病种、药物、时间、患者特征和结局筛选队列。
- 设置纳入标准、排除标准和观察时间窗。
- 定义暴露、结局、分组变量和混杂因素。
- 查看样本量、缺失值、异常值和数据来源。
- 冻结某次分析使用的数据集版本。
- 导出经过授权和脱敏的研究数据。

### 8.8 科研报告草稿

报告工作台应输出：

- 研究问题和研究设计说明。
- 纳入排除流程和患者基线特征。
- 用药暴露和观察窗口。
- 疗效及安全性结果。
- 缺失数据和异常数据说明。
- 统计表、图表和统计方法记录。
- 数据限制、可能偏倚和适用范围。
- AI 辅助生成的结论草稿。
- 审核意见、版本和冻结记录。

系统不得将报告草稿标记为“可直接发表的最终论文”。正式研究仍需完成医院要求的研究方案、统计审核、伦理流程及作者审核。

### 8.9 知识库审核

知识内容分为：

1. 原始证据库：指南、说明书、临床路径和院内制度。
2. 院内经验库：经过医生或药师审核的经验和风险事件。
3. 科研结论库：经过数据分析及专业审核的研究结论。

正式知识结论必须包含：

- 结论内容及适用范围。
- 数据集和报告版本。
- 样本量、纳入条件和排除条件。
- 证据等级或可信程度。
- 已知限制和不适用范围。
- 审核人、时间和意见。
- 生效、失效、撤回和重新审核状态。

### 8.10 证据资料中心

指南、说明书和院内资料采用以下受控流程：

```text
上传原始文件
  -> 文件安全检查和哈希计算
  -> 本地文档解析或 OCR
  -> 标题、章节、表格、页码和坐标识别
  -> 自动切分证据片段并补充元数据
  -> 重复文件和新旧版本检测
  -> 药师或资料管理员审核
  -> 发布到正式证据库
```

- 支持 PDF、扫描图片、DOCX、XLSX、TXT 和 Markdown。
- 资料状态包括待解析、解析失败、待补充元数据、待审核、已发布、已替代和已撤回。
- 保留原始文件、解析结果和证据片段三层内容。
- 未审核资料不得参与正式处方推荐。
- 新版本发布后不删除旧版本，并标记可能受影响的规则和知识结论。
- 解析失败、低置信度和高风险表格进入人工处理队列。

## 9. 数据状态与可信边界

页面和数据层必须明确区分：

- 原始事实：来自院内系统或人工录入的记录。
- 结构化结果：从原始记录提取并等待确认的数据。
- 规则结果：确定性规则或计算函数产生的结果。
- AI 草稿：模型生成且尚未专业审核的内容。
- 已审核结论：授权人员确认并冻结的内容。

AI 草稿和已审核医学结论不得显示为同一种状态。

## 10. 第一版页面范围

### 10.1 患者工作列表

- 按风险、推荐状态、入院时间和主管医生筛选。
- 展示诊断、关键风险、待处理事项和最新状态。

### 10.2 患者用药全景页

- 汇总诊断、检验、过敏、历史和当前用药。
- 优先展示会改变当前处方决策的信息。
- 支持查看趋势、原始记录和数据来源。
- 标记信息缺失、冲突和待确认状态。

### 10.3 处方推荐与审核页

- 展示候选方案和推荐排序。
- 展示推荐原因、同类药对比、风险和证据。
- 展示被排除药物及原因。
- 支持采纳、修改、驳回和填写理由。
- 最终提交前执行处方安全审核。

### 10.4 患者用药时间线

- 展示跨就诊周期的用药事件。
- 关联用药变化、检验变化、不良反应和结局。
- 再次入院时突出历史风险及其对当前推荐的影响。

### 10.5 科研工作台

- 创建研究队列并配置变量和观察窗口。
- 查看数据质量、统计表和图表。
- 生成报告草稿并发起审核。

### 10.6 知识审核中心

- 对照原始数据、报告和证据审核经验与研究结论。
- 支持通过、退回、撤回和重新审核。
- 管理结论版本和适用范围。

导航、信息密度、桌面分辨率适配和具体交互将在 UI 头脑风暴阶段确认。

## 11. 核心数据对象

- `PatientProfile`：患者主索引和长期风险摘要。
- `Encounter`：一次门诊、急诊或住院周期。
- `DepartmentParticipation`：同一次就诊中的主管科室、会诊科室、转科和参与时间。
- `SourceIdentifierMapping`：内部患者及就诊标识与 HIS、EMR、LIS 等来源标识的映射。
- `Diagnosis`：诊断及来源和状态。
- `Observation`：症状、体征、检查和检验结果。
- `MedicationOrder`：医生计划开立的处方或医嘱。
- `MedicationExposure`：患者实际发生的用药暴露。
- `MedicationPlan`：系统生成的候选用药方案。
- `RecommendationDecision`：推荐、审核及医生最终决定。
- `SafetyAlert`：风险规则命中和处理结果。
- `AllergyEvent`：药物过敏事件及确认状态。
- `AdverseDrugReaction`：不良反应及专业审核状态。
- `MedicationFeedback`：疗效、无效、停药和换药反馈。
- `DischargeOutcome`：出院状态、带药和随访计划。
- `EvidenceDocument`：指南、说明书、路径和院内制度。
- `ResearchCohort`：研究队列定义。
- `ResearchDataset`：冻结的科研数据集版本。
- `ResearchReport`：报告草稿、审核和冻结版本。
- `KnowledgeConclusion`：经过审核的知识结论。
- `AuditLog`：关键数据和操作的审计记录。

字段、编码体系和医院接口映射将在技术选型确定后形成数据字典。

## 12. 非功能需求

### 12.1 安全与权限

- 按角色和科室控制数据访问。
- 对敏感患者数据进行传输和存储保护。
- 研发和演示环境使用脱敏或合成数据。
- 记录推荐、审核、知识变更和数据导出的审计日志。
- 不在代码、日志或模型提示中泄露密钥和非必要身份信息。

### 12.2 可追溯性

- 推荐可追溯到患者数据、规则和证据版本。
- 医生修改可追溯到操作人和修改原因。
- 科研结论可追溯到队列、数据集、统计方法和审核记录。
- 指南更新后可以定位受影响的规则和知识结论。

### 12.3 可配置性

- 病种、科室、药品目录和风险等级可配置。
- 指南、说明书和院内规则支持版本管理。
- 试点病种可替换，不需要重写整套产品。
- 医院接口差异通过适配层处理。

### 12.4 可用性和可靠性

- 首屏优先展示影响当前处方的关键信息。
- 医生能够用较少操作完成查看、对比和确认。
- 高风险提醒清晰，同时避免提醒疲劳。
- 模型不可用时，患者原始信息和确定性风险检查仍可使用。
- 接口失败时显示缺失项和更新时间，不伪造完整结果。
- 推荐结果按患者事实、硬规则、候选证据和 AI 解释逐步展示。
- AI 服务失败时，患者事实和确定性安全检查仍可使用。
- 页面不显示无限加载状态，必须展示当前阶段、耗时和失败原因。

### 12.5 部署与运维

- 第一版以 Linux x86_64 容器部署为基线，使用 Docker Compose 或医院允许的兼容工具。
- 保持容器、数据库、模型网关和文件存储接口可替换，国产化适配等待医院提供明确清单后验证。
- 应用输出结构化日志、运行指标和链路标识，通过 OpenTelemetry 接入医院现有监控。
- 数据库和文件使用同一备份批次，定期执行恢复演练。
- RPO、RTO 和审计保存期限由医院制度最终确认。
- 审计日志只追加，业务管理员和超级管理员均不能删除。

## 13. MVP 验收标准

### 13.1 完整业务闭环

1. 导入患者、诊断、检验、历史用药和药品目录。
2. 形成带来源标记的患者上下文。
3. 生成 2 至 3 个带证据和解释的候选方案。
4. 过滤已经确认过敏或明确禁用的药物。
5. 医生采纳、修改或驳回并记录原因。
6. 对最终处方进行安全审核。
7. 记录实际用药、调整、反馈和出院结局。
8. 再次入院时继承历史风险并影响新推荐。
9. 创建研究队列并生成可追溯的报告草稿。
10. 报告经审核后生成知识结论，未审核内容不能进入正式库。

### 13.2 安全和数据验收

- 已确认过敏药物在二次推荐中的阻断率为 100%。
- 所有关键剂量计算都有确定性规则或函数来源。
- 每条正式推荐都有患者依据、证据版本和规则记录。
- 高风险提醒被覆盖时保留人员、时间和理由。
- 未审核研究结论无法成为正式推荐依据。
- 患者可关联多次就诊及多次用药事件。
- 原始数据、结构化结果和 AI 草稿状态可区分。
- 冻结的数据集能够复现当次报告的数据口径。

### 13.3 可用性验收

- 医生能够快速找到改变当前处方决策的关键信息。
- 医生能够理解推荐和排除理由。
- 药师能够查看和处理风险提醒。
- 科研人员能够识别样本口径、数据缺失和报告限制。

### 13.4 性能与降级验收

- 患者核心信息目标在 2 秒内出现。
- 患者数据就绪后，硬规则目标在 1 秒内返回。
- 首个候选方案和证据目标在 5 秒内出现。
- 完整候选方案目标在 15 秒内完成。
- AI 解释允许流式展示，30 秒后触发超时降级。
- 以上为开发期产品目标，医院硬件确定后再冻结正式性能指标。
- AI 失败时可以继续使用患者全景和基础安全审查。
- 证据检索失败时不得生成无来源推荐理由。

### 13.5 测试与交付验收

- 建立单元测试、接口契约测试、医学规则病例测试、模型回归评测和 Playwright 端到端测试。
- 已知过敏和明确禁忌的阻断率必须达到 100%。
- 推荐均带可定位证据，跨就诊风险可以继承，科研结果可以追溯。
- 交付物包括可运行系统、HIS 模拟器、模拟病例集、规则测试集、数据字典、接口文档、部署文档和验收报告。

## 14. 第一版不做

- 不自动诊断疾病。
- 不替代医生作出最终处方决定。
- 不在无人审核情况下写入正式医嘱。
- 不一次覆盖全院、全部科室和全部病种。
- 不让大模型自由生成关键剂量或硬性医学规则。
- 不自动认定所有疑似不良反应的因果关系。
- 不把个案经验直接升级为群体结论。
- 不承诺自动生成可直接投稿的最终论文。
- 不让未经审核的结论直接反哺处方推荐。

## 15. 已确认技术架构

### 15.1 交付与集成形态

- 第一版采用独立院内 Web 工作台。
- 从 HIS 携带当前患者和就诊上下文进入。
- 预留单点登录、页面嵌入和处方结果回写能力。
- 在医院接口尚未完成时，可以使用模拟或脱敏数据独立运行。

### 15.2 前端技术

- Vue 3。
- TypeScript。
- Vite。
- Pinia。
- Vue Router。
- Element Plus 作为基础组件库。
- `lucide-vue-next` 作为图标库。
- 使用自定义医疗工作台主题，不直接套用通用后台模板。
- Vitest 用于单元测试，Playwright 用于页面流程验证。

### 15.3 后端技术

- Spring Boot 作为正式业务核心。
- Java 服务负责患者、处方、规则、剂量计算、权限、审核、审计、科研版本和 HIS 接口。
- FastAPI 作为独立 AI 服务。
- Python 服务负责文本解析、RAG 检索、证据重排序、推荐解释和报告草稿。
- Java 与 Python 通过版本化 OpenAPI 接口通信。
- 第一版采用模块化单体业务核心，不拆分大量微服务。
- Python AI 服务不能直接写正式处方、确认过敏或发布知识结论。

### 15.4 数据与检索

- PostgreSQL 作为统一业务和科研数据底座。
- pgvector 保存证据向量并提供向量检索。
- PostgreSQL 全文检索用于关键词召回。
- JSONB 用于保存有明确版本约束的扩展结构，不替代核心关系模型。
- 原始指南、说明书和附件放在医院内网对象存储中。
- 第一版不引入独立向量数据库或 Elasticsearch 集群。

### 15.5 模型部署

- 研发环境使用模拟或脱敏数据调用云端 DeepSeek API。
- 医院环境使用真实数据连接院内本地 DeepSeek 服务。
- 所有模型通过统一模型网关调用，不绑定具体 DeepSeek 版本。
- 模型网关提供云端研发适配器和院内本地适配器。
- 本地推理服务优先采用 OpenAI 兼容接口，具体框架根据医院算力确定。
- 生成模型、Embedding 模型和重排序模型职责分离。
- 提示词、模型、检索参数和输出均保留版本。
- 使用固定脱敏病例集对云端和本地模型执行回归评测。

### 15.6 推荐引擎

处方推荐采用固定可审计流水线：

```text
读取患者数据
  -> 结构化并标记缺失项
  -> 执行过敏和禁忌硬规则
  -> 检索指南、说明书和院内路径
  -> 从院内药品目录生成候选药
  -> 确定性剂量计算
  -> 比较和排序候选方案
  -> 大模型生成推荐解释
  -> 再次执行安全校验
  -> 医生审核
```

正式处方推荐主链路不采用能够自主规划和自由调用工具的通用医疗 Agent。

### 15.7 医学规则引擎

- 使用可视化医学决策表表达条件、操作、风险等级和提示文案。
- 使用 Java 确定性计算函数处理剂量、上限和需要精确计算的医学指标。
- 规则分为硬规则、强提醒和一般提示，并与页面风险等级保持一致。
- 规则必须包含来源、版本、适用范围、生效时间和失效时间。
- 规则修改先进入草稿，经过药师或授权专家审核后发布。
- 每条生产规则配置正例、反例和边界病例测试。
- 已发布规则不能直接覆盖，只能创建新版本并保留历史版本。
- 规则执行结果记录当时使用的版本，以支持复现和审计。
- 不允许大模型或管理员自由脚本直接生成或修改生产规则。

### 15.8 RAG 证据检索

RAG 采用以下混合检索流程：

```text
患者问题和候选药物
  -> 按病种、人群、证据状态和文档版本过滤
  -> 关键词检索和向量检索并行召回
  -> 合并去重
  -> 重排序模型重新打分
  -> 提取可定位的证据片段
  -> 大模型根据证据片段生成解释
```

- 每条证据保留文档名称、发布机构、版本和有效期。
- 每条证据可以定位到页码、章节、表格或原文位置。
- 保存原始片段、检索分数、重排序分数和实际引用状态。
- 权威证据冲突时同时显示冲突，并按照医院确认的优先级处理。
- 找不到足够证据时明确显示“证据不足”，禁止模型补写推荐理由。

### 15.9 院内文档解析

文档解析服务部署在医院内网，不调用云端 OCR 或云端文档解析 API。

- PDF 先检测文本层和页面结构。
- 可可靠读取的电子 PDF 优先原生提取，保留页码、字符位置和阅读顺序。
- 扫描页、乱码页、复杂分栏和复杂表格使用 PaddleOCR 与 PP-Structure 本地解析。
- DOCX 使用 `python-docx` 原生读取标题、段落和表格。
- XLSX 使用 `openpyxl` 原生读取工作表、单元格、公式和合并关系。
- TXT 和 Markdown 直接解析。
- 第一版不同时维护 MinerU、Docling 等多套生产解析器；它们只作为评测或替换候选。
- PaddleOCR 模型、字体和运行依赖随院内部署包预置，运行时不从外网下载。

所有解析器统一输出 `DocumentBlock`：

- 文档 ID、文档版本和文件哈希。
- 页码、章节路径和内容类型。
- 原文文本、表格结构和页面坐标。
- 解析器名称、解析器版本和解析方式。
- 解析置信度、人工确认状态和错误信息。

发布前使用抽样页面和复杂表格执行原文对照检查；重要剂量表、禁忌表和特殊人群表必须人工确认。

### 15.10 文件与附件存储

- PostgreSQL 保存文件元数据、哈希、版本、权限和业务引用关系。
- 原始指南、扫描件、报告附件和解析产物保存在医院 NAS 或受控文件目录。
- 业务模块通过统一 `BlobStorage` 接口访问文件，不直接拼接或操作物理路径。
- 开发环境使用本地文件目录，医院环境可以切换到 NAS 或已有的 S3 兼容存储。
- 文件使用内容哈希目录、只追加版本和软删除机制。
- 原始文件、解析结果和数据库备份使用同一备份批次编号，支持一致性恢复。
- 第一版不强绑定 MinIO 或其他特定对象存储产品。

### 15.11 异步任务

- 第一版使用 PostgreSQL 可靠任务表，不增加 Kafka、RabbitMQ 或 Redis 消息队列。
- `async_job` 保存任务类型、输入版本、状态、优先级、幂等键和创建人。
- 任务状态包括等待、执行中、成功、失败、已取消和死信。
- Worker 通过数据库行锁和任务租约避免重复执行。
- 执行中记录心跳，Worker 异常后任务可以自动恢复。
- 支持超时、重试、退避、手动重跑和取消。
- OCR、Embedding、模型推理和报告生成使用不同并发上限。
- Python Worker 只能写任务草稿结果，正式状态由 Java 核心服务确认。
- 通过 `JobDispatcher` 接口保留未来切换专业消息队列的能力。

### 15.12 HIS 认证、上下文与处方回写

认证与上下文传递：

- 医院支持 OIDC 时优先接入 OIDC 单点登录。
- 医院使用 CAS、SAML 或自有认证时，通过认证适配器接入。
- 开发环境提供独立测试账号和角色模拟器。
- HIS 使用短期、一次性、签名票据传递医生、患者和就诊上下文。
- 票据包含签发时间、过期时间、随机数和目标系统，用后失效。
- Java 后端验证票据后，从本地权限映射加载角色，不信任前端传入角色。
- URL 中不得携带患者姓名、诊断或可长期使用的访问令牌。
- 切换患者时必须获取新的上下文票据。

处方回写：

```text
医生采纳或修改 HospitalAI 候选方案
  -> Java 后端再次执行安全校验
  -> 调用 HIS 官方接口创建处方草稿
  -> 医生在 HIS 原生界面最终确认
  -> HIS 返回正式医嘱编号和状态
  -> HospitalAI 同步最终结果
```

- 禁止直接修改 HIS 数据库表。
- 每次回写携带患者、就诊、医生、推荐版本和幂等键。
- 回写前再次验证当前患者、就诊和数据版本。
- 回写失败时保留草稿和错误原因，不显示虚假成功状态。
- 只有 HIS 返回的正式医嘱才视为最终处方。
- 医院暂时没有写入接口时，降级为结构化预览和人工录入。

数据同步：

- 从 HIS 进入患者工作台时实时读取一次患者快照。
- 优先接收检验、医嘱、过敏和转科等变更事件。
- 医院不支持事件通知时，对当前打开患者进行轻量轮询。
- 生成推荐前刷新过敏、当前医嘱、关键检验和就诊状态。
- 提交处方草稿前再次刷新并比较数据版本。
- 关键数据变化后，原推荐标记为已过期并要求重新计算或重新审核。
- 历史病历和非关键资料后台增量同步，不阻塞当前开方。
- 接口异常时明确显示无法更新的数据范围和最后成功时间。

### 15.13 模拟 HIS 与接口契约

第一开发阶段通过模拟数据导入和 HIS 模拟器完成闭环，不等待真实医院接口。

- 使用版本化标准 JSON 契约表达患者、就诊、诊断、检验、过敏、医嘱、实际用药和药品目录。
- 模拟器支持首次快照、增量事件、轮询响应、处方草稿回写和最终医嘱回调。
- 测试数据覆盖正常、关键字段缺失、跨科室冲突、重复事件、乱序事件、超时和接口失败。
- 医院接入时只新增认证、字段映射和传输适配器，内部数据模型及推荐主链路保持不变。
- 接口字段按医院维护独立映射版本，医院字段变化不能直接修改内部统一模型。
- 以医院患者主索引和就诊标识为准，无法确认的映射进入人工处理队列。
- 禁止仅凭姓名、手机号或证件号码自动合并患者。

### 15.14 计算、检索与缓存

- 按药品、适应证和特殊人群建立独立剂量计算器，统一处理单位、精度、上下限和异常输入。
- 证据层级由医院配置；硬性安全规则优先，证据冲突时同时展示。
- 指南更新后执行影响分析，将相关规则、推荐和知识结论送入人工复核，不自动修改生产规则。
- Embedding 和重排序模型通过中文医学检索测试集评测后选择，不提前绑定热门模型。
- 第一版仅使用 Java 本地缓存处理药品字典等低风险静态数据，不增加 Redis。
- 患者过敏、当前医嘱和其他安全数据在提交前重新读取，不依赖可能过期的缓存。

### 15.15 科研计算与知识审核

- Python 使用固定分析代码和版本化参数执行统计，AI 只能解释结果，不能自行选择统计方法。
- 研究编号与院内患者号分离，映射表单独保护；导出前再次执行字段脱敏和权限审核。
- 知识审核按内容类型配置多人流程。
- 医学规则至少由药师和临床专家审核。
- 科研结论由研究负责人审核，统计方法根据医院要求增加统计人员复核。
- 社区获得性肺炎专病模板先提供字段框架，疗效指标、时间窗口和判定标准由合作医院专家确认。
- 科研输出先生成可复现数据包、统计表、图表和报告草稿，再导出 Word。

### 15.16 开发与发布策略

- 第一条开发主线采用纵向切片：模拟 HIS 患者 -> 上下文 -> 硬规则 -> 证据检索 -> 候选方案 -> 医生审核。
- 直接开发可运行的 Vue 交互原型并连接 HIS 模拟器，不等待全量静态设计稿完成。
- 前后端接口采用 OpenAPI 契约并执行自动契约测试。
- 每次模型、提示词、规则或检索参数变更后执行固定病例回归评测。
- 未通过医学规则测试、安全测试和关键端到端流程的版本不能进入试点环境。

## 16. 已确认 UI 与交互

### 16.1 医生核心工作台

- 医生从 HIS 进入后直接打开当前患者，不经过多步向导。
- 页面采用“患者上下文 + 候选方案 + 风险证据”三栏单屏布局。
- 左栏约 28%，中栏约 47%，右栏约 25%。
- 中栏是视觉主区，窄屏时右栏收为抽屉。
- 默认按医院常见桌面环境设计，并适配嵌入 HIS 后的较窄可用宽度。

### 16.2 候选方案

- 使用横向方案对比矩阵，不使用三个独立大卡片。
- 方案命名为“推荐方案、备选方案、特定条件方案”。
- 不使用“最佳方案”等容易造成过度依赖的表述。
- 对比项包括药品组合、适用理由、同类药差异、主要风险、监测要求和证据等级。

### 16.3 风险交互

- 阻断风险：红色常驻区域，不允许直接提交。
- 强提醒：橙色行内提示，继续操作时必须说明理由。
- 一般提示：中性提示，不打断开方流程。
- 合并同一药品的相关提醒，减少连续弹窗和提醒疲劳。
- 最终提交时只汇总尚未处理的阻断和强提醒。

### 16.4 患者上下文

- 按是否影响本次处方决策排序，不复制 HIS 原有病历目录。
- 顶部固定展示过敏、严重不良反应、特殊人群和当前阻断风险。
- 其次展示当前诊断、关键检验趋势、肝肾功能和生命体征。
- 再展示当前用药、近期停药和历史有效或无效方案。
- 单独展示缺失、冲突、过期和待确认数据。
- 每条信息展示来源和时间，并可返回原始记录。

### 16.5 长期追踪

- 按就诊分段展示患者长期用药时间线。
- 每个就诊区段包含用药、检验、症状、不良反应、医生调整和结局等并行轨道。
- 历史风险影响当前推荐时，直接展示事件与当前规则之间的关联。

### 16.6 科研工作台

科研工作台采用阶段式流程：

```text
研究方案
  -> 研究队列
  -> 变量字典
  -> 数据质量
  -> 统计分析
  -> 报告草稿
  -> 审核冻结
  -> 知识提交
```

- 每个阶段显示当前数据集版本。
- 上游条件改变后，下游结果自动标记为需要重新计算。
- 未冻结的数据集和未审核报告不能形成正式知识结论。

### 16.7 全局导航与角色首页

- 系统按角色显示导航，不向所有用户展示相同的功能总菜单。
- 医生默认进入当前患者或“我的待处理”。
- 药师默认进入审方队列和风险复核。
- 科研人员默认进入研究项目和报告任务。
- 知识审核人员默认进入待审核结论。
- 管理员默认进入接口状态、配置和审计。
- 超级管理员可以访问所有角色的页面，并通过明确的角色视角切换器检查不同工作区。
- 首页以待办任务为主，不使用缺少操作价值的统计大屏作为默认首页。

### 16.8 渐进式推荐结果

推荐页面按以下顺序逐步展示：

1. 患者身份、诊断、过敏、当前用药和关键检验。
2. 过敏、禁忌、重复用药和跨科室冲突等确定性规则。
3. 候选药物、指南证据和排除原因。
4. 方案比较、推荐理由和 AI 自然语言解释。

- 后续阶段失败不清空已经成功展示的前序结果。
- 每个阶段展示状态、耗时、数据版本和失败原因。
- AI 失败后页面自动降级为患者全景与安全审查工作台。

### 16.9 推荐修改与证据查看

- 医生在方案对比矩阵中直接修改，不跳转到独立编辑页面。
- 页面自动展示修改前后差异。
- 涉及风险、剂量或方案变化时，根据风险等级要求填写原因。
- 右侧证据面板同时展示证据片段、文档信息、页码和原始 PDF 页面。
- 点击证据可以定位并高亮原文，不能只展示脱离来源的提取文字。

### 16.10 通知、分辨率与可访问性

- 第一版只开发系统内待办中心，并预留 HIS 消息接口，不开发短信、微信或移动端通知。
- 第一版优先医院桌面端，最低可用宽度为 1366，重点优化 1920x1080 和 HIS 嵌入宽度。
- 第一版不承诺完整手机端和平板端体验。
- 风险状态同时使用颜色、图标、文字和等级表达，不能只依赖颜色。
- 键盘焦点、表单标签、对比度和表格阅读顺序纳入页面验收。

## 17. 医院接入前待确认事项

以下事项不阻塞模拟数据阶段开发，待合作医院提供真实环境后确认：

1. HIS、EMR、LIS、药品目录和处方回写的真实接口协议与字段。
2. 患者主索引、就诊标识和跨系统标识的医院权威来源。
3. 医院单点登录协议、身份目录和角色映射。
4. 医院服务器、GPU、CPU 架构、操作系统、容器平台和网络限制。
5. 国产化软硬件和数据库适配清单。
6. 医院 NAS 或对象存储、备份平台、RPO、RTO 和恢复演练要求。
7. 日志、监控、审计保存期限和安全审查要求。
8. 社区获得性肺炎专病指标、观察时间窗和临床判定标准。
9. 医学规则、知识结论和科研报告的正式审核角色。
10. 可用于试点的脱敏病例、指南、药品目录及伦理或科研管理要求。

## 18. 相关资料

- [项目工作约定](Cluade.md)
- [当前进度](Memory.md)
- [项目常识库](Wiki.md)
- [处方推荐、随访与科研沉淀方向](docx/集中开发方向_处方推荐_随访科研沉淀.md)
- [双核心功能 OKR 与 MVP 边界](docx/大型医院药学AI_双核心功能OKR与MVP边界.md)
