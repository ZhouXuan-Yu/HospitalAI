# HospitalAI 项目常识库

更新时间：2026-08-03

## 项目背景

HospitalAI 是面向医院的 AI 药学辅助系统，目标是提升医生开方效率、降低用药风险、沉淀院内药学经验，并支持科室科研数据整理。

系统将来可能与医院 HIS 对接，结合院内 AI 模型、医疗指南、药品说明书、临床路径、医院药品目录和医生知识库，为医生和药师提供辅助决策。

## 核心用户

- 临床医生：查看推荐方案，采纳、修改或驳回。
- 药师：查看风险提示，参与处方审核和处方点评。
- 科室主任或科研负责人：查看沉淀数据和科研报告。
- 信息科：负责 HIS 接口、权限、安全、部署环境。

## 核心业务闭环

```text
患者数据 -> 患者上下文结构化 -> 规则检查 -> 证据检索 -> 推荐生成 -> 医生审核 -> 实际用药 -> 反馈和结局 -> 数据沉淀 -> 科研报告 -> 审核入库 -> 反哺推荐
```

## 关键业务口径

### 处方推荐

处方推荐是医生辅助工具，不是自动开方。

推荐结果需要包含：

- 药品名称。
- 规格。
- 剂量。
- 频次。
- 疗程。
- 推荐理由。
- 证据来源。
- 风险提示。
- 被排除药品及排除原因。
- 医生审核结果。

### 患者长期追踪

患者长期追踪指同一患者跨多次就诊或住院周期的药学档案。

需要追踪：

- 历史诊断。
- 历史用药。
- 医生修改。
- 实际执行。
- 疗效反馈。
- 不良反应。
- 过敏事件。
- 停药和换药原因。
- 出院结局。
- 再入院原因。

明确过敏或严重不良反应应影响后续推荐。

### 科研沉淀

科研沉淀不是简单报表，而是面向医学研究的数据和结论整理。

至少需要保留：

- 研究队列。
- 纳入标准。
- 排除标准。
- 样本量。
- 变量定义。
- 暴露定义。
- 结局定义。
- 随访窗口。
- 缺失数据说明。
- 统计方法。
- 数据来源。
- 审核记录。
- 报告版本。

## 知识库分层

建议知识库分为三层：

1. 原始证据库
   - 医疗指南。
   - 药品说明书。
   - 临床路径。
   - 法规文档。

2. 院内经验库
   - 医生审核后的用药经验。
   - 药师审核后的处方点评结论。
   - 确认过的不良反应和过敏事件。

3. 科研结论库
   - 经过统计分析和人工审核的研究结论。
   - 可追溯的数据口径。
   - 可用于论文支撑的图表和报告版本。

## 推荐系统硬规则

这些规则不能交给大模型自由处理：

- 过敏药物黑名单过滤。
- 禁忌症过滤。
- 重复用药检查。
- 药物相互作用检查。
- 肝肾功能剂量调整。
- 儿童、老人、妊娠等特殊人群规则。
- 说明书剂量上限。

## 可能的 HIS 对接信息

第一版可能需要读取：

- 患者基本信息。
- 入院诊断。
- 既往病史。
- 过敏史。
- 检查检验结果。
- 当前医嘱。
- 历史医嘱。
- 药品目录。
- 出院记录。

第一版不建议直接做无审核自动写入医嘱。

最新商业化开发口径：

- 功能实现以真实数据链路为准：连接器、离线导入器、字段映射、版本控制、可靠任务、数据库状态、权限、审计和处方草稿回写状态机必须真实打通。
- 模拟或合成病例只允许作为“导入后的验证数据集”，用于复现正常、缺失、冲突、重复、乱序、超时和接口失败等测试场景，不能作为前端或业务 API 的 Mock 替身。
- 没有医院真实 HIS 环境时，先实现标准化适配器接口、文件/API 导入链路和可审计回写协议；医院接入时替换认证、字段映射和传输适配器，不修改内部统一模型和推荐主链路。
- HospitalAI 只回写处方草稿和审核状态，不直接写正式医嘱；正式医嘱仍由医生在 HIS 原生流程中确认。
- 与旧 MVP 文档冲突时，按最新用户指令优先：真实数据链路优先，模拟器仅保留为验收与回归测试手段。

## 已冻结技术基线

- 前端：Vue 3、TypeScript、Vite、Pinia、Element Plus 自定义医疗主题。
- 业务核心：Spring Boot 模块化单体。
- AI 服务：FastAPI，负责解析、检索、重排序和解释草稿。
- 数据库：PostgreSQL + pgvector。
- 文件：PostgreSQL 管元数据，文件目录或 NAS 保存原始附件，通过 BlobStorage 接口访问。
- 异步任务：PostgreSQL 可靠任务表 + Java/Python Worker。
- 模型：统一模型网关；研发使用模拟数据和云端 API，医院使用院内本地 DeepSeek。
- 文档解析：电子文档优先原生解析，扫描件和复杂表格使用本地 PaddleOCR/PP-Structure。
- 部署基线：Linux x86_64 容器；医院环境通过适配器处理 SSO、HIS 和基础设施差异。

## AI 与正式业务边界

- AI 可以整理患者上下文、检索证据、生成候选解释和报告草稿。
- AI 不能直接写正式处方、确认过敏、发布医学规则或发布知识结论。
- 过敏、禁忌、相互作用、剂量上限和精确计算由 Java 确定性规则处理。
- Python 服务只能写草稿结果，正式状态由 Java 核心服务和授权人员确认。
- 找不到足够证据时显示“证据不足”，禁止模型补写推荐理由。

## 数据合规常识

- 试点病例必须脱敏。
- 真实患者身份信息不能进入演示数据。
- 研究数据需要版本化。
- 知识库结论需要审核状态。
- 医学发表需要考虑伦理审批或豁免。
- 原始数据、清洗数据、分析结果、报告版本之间要可追溯。

## 当前推荐的第一版页面

1. 患者列表。
2. 患者上下文页。
3. 处方推荐审核页。
4. 患者随访时间线。
5. 科研报告工作台。
6. 证据资料中心。
7. 知识审核中心。
8. 角色待办中心。
9. 管理与超级管理员调试页面。

## 当前推荐的核心数据对象

- `PatientProfile`：患者主索引。
- `Encounter`：一次就诊或住院周期。
- `DepartmentParticipation`：主管科室、会诊、转科和参与时间。
- `SourceIdentifierMapping`：内部标识与 HIS、EMR、LIS 来源标识映射。
- `Diagnosis`：诊断。
- `LabResult`：检查检验结果。
- `MedicationOrder`：医嘱或处方。
- `MedicationExposure`：实际用药暴露。
- `RecommendationDecision`：AI 推荐和医生审核。
- `MedicationFeedback`：用药反馈。
- `AllergyEvent`：过敏或疑似过敏事件。
- `AdverseDrugReaction`：不良反应。
- `DischargeOutcome`：出院结局。
- `ResearchDataset`：科研数据集。
- `ResearchReport`：科研报告。
- `KnowledgeConclusion`：审核后的知识库结论。

## 当前工程结构

- `apps/web`：Vue 3 + TypeScript + Vite + Pinia + Vue Router + Element Plus 医生三栏工作台。
- `services/core-api`：Spring Boot 模块化单体雏形，负责患者上下文、硬规则、候选方案、医生审核、审计和 HIS 草稿模拟回写。
- `services/ai-service`：FastAPI 受控证据检索和解释草稿服务；当前为确定性演示实现。
- `fixtures/his`：版本化合成 HIS 快照。
- `contracts`：OpenAPI 和 JSON Schema 契约。
- `infra`：PostgreSQL/pgvector Docker Compose 与初始化脚本。
- `docs/validation`：阶段验收记录和截图。

本机开发可使用 `h2-demo` profile 演示闭环，但项目生产和正式集成基线仍是 PostgreSQL + pgvector。H2 只用于无 Docker daemon、无 PostgreSQL 凭据时的本地演示，不替代正式验收。
