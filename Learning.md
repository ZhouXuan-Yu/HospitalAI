# HospitalAI 复盘记录

更新时间：2026-08-03

## 复盘目的

本文件记录项目推进中出现的问题、误区、原因和下次改进方式。

它不是需求文档，而是为了避免后续 agent 和开发者重复犯错。

## 已发现的风险和改进

### 1. 容易把系统做得过大

问题：

- 医院 AI 药学系统天然会牵涉 HIS、EMR、LIS、药品目录、知识库、科研、随访、小程序等很多模块。
- 如果第一版直接承诺全院、全科室、全病种，开发和试点都会失控。

原因：

- 原始需求覆盖面较广。
- 大型医院场景容易让人追求“大而全”。

下次改进：

- 第一版只选一个科室、一个病种、10 到 20 条脱敏病例。
- 验收重点放在闭环跑通，不放在覆盖范围。

### 2. 容易把 AI 推荐误解成 AI 开方

问题：

- 处方推荐如果表达不当，容易被理解为 AI 替医生开药。
- 这在医疗安全、合规、医院接受度上都有风险。

原因：

- “智能处方推荐”这个名称容易被外部理解为自动决策。

下次改进：

- 对外统一描述为“医生/药师可审核的处方推荐工作台”。
- 明确 AI 只给建议，医生或药师做最终决策。
- 所有推荐保留证据链和审核记录。

### 3. 容易让大模型承担不该承担的医学硬规则

问题：

- 过敏、禁忌、相互作用、剂量上限、肝肾功能调整等不能靠大模型自由判断。

原因：

- 大模型擅长解释和组织信息，但不适合作为确定性安全规则执行器。

下次改进：

- 安全规则、剂量计算、黑名单过滤必须进入规则引擎或确定性函数。
- 大模型只负责候选方案解释、证据整合、报告草稿生成等软决策部分。

### 4. 容易把科研报告做成普通业务报表

问题：

- 用户明确说报告会用于医学文章发表的数据支撑。
- 普通报表只展示统计数字，不足以支撑科研发表。

原因：

- 科研报告需要纳入排除标准、变量定义、暴露定义、结局定义、随访窗口、缺失数据说明、统计方法、审核版本。

下次改进：

- 科研报告第一版必须先生成“可审核数据包 + 报告草稿”。
- 不承诺直接生成可投稿论文。
- 审核冻结后，结论才能进入知识库。

### 5. 文件命名需要尊重用户输入

问题：

- 用户要求创建 `Cluade.md`，这可能是 `Claude.md` 的拼写误差。

处理：

- 本次按用户原始要求创建 `Cluade.md`。

下次改进：

- 如需要兼容其他工具，可再询问是否额外创建 `Claude.md` 或重命名。

## 后续每次复盘建议记录

每次阶段推进后，补充：

- 本次做了什么。
- 哪些判断后来证明不对。
- 错误原因是什么。
- 下次如何避免。
- 是否影响产品边界、数据模型或医院试点口径。

### 6. 本地验证不能假装等同生产 PostgreSQL 验证

问题：

- 本机 Docker Desktop Linux engine 未运行，PostgreSQL 服务虽在 5432 监听但没有可用 postgres 凭据。
- 为了完成本轮演示，使用了 Spring `h2-demo` profile。

下次改进：

- 明确标注 H2 只代表本地演示闭环，不代表 PostgreSQL/pgvector 集成验收。
- 获取 PostgreSQL 凭据或启动 Docker Desktop 后，必须补跑真实 PostgreSQL 初始化、落库、回读和回归测试。

### 7. 医疗中文数据必须显式锁定 UTF-8

问题：

- 初次 H2 初始化后中文字段出现乱码，导致 AI 证据服务按诊断匹配失败。

原因：

- Maven/Spring SQL 初始化没有显式声明 UTF-8。

下次改进：

- `pom.xml` 和 `spring.sql.init.encoding` 必须显式使用 UTF-8。
- API 验收用 Node 或浏览器读取 JSON，避免 PowerShell 控制台编码误导。

### 8. Java 到 FastAPI 的本地 HTTP 调用需强制 HTTP/1.1

问题：

- Java `HttpClient` 默认 HTTP/2 upgrade，Uvicorn 本地日志出现 `Unsupported upgrade request`，FastAPI 收到空 body 并返回 422。

下次改进：

- 对 FastAPI 本地服务调用强制 `HttpClient.Version.HTTP_1_1`。
- 跨服务请求使用明确 DTO 和错误详情，不用临时 Map 靠隐式序列化。

### 9. 商业化口径升级时必须显式处理与 MVP 冲突

问题：

- 早期 MVP 口径允许第一阶段使用 HIS 模拟器和合成病例开发。
- 用户后续要求以“完整的大型商业千万级项目”为对标，且开发阶段不得使用 Mock 或模拟数据作为功能替身。

原因：

- MVP 验证目标和商业交付目标不同；如果不显式记录优先级，后续 agent 容易继续按旧的模拟器优先路线推进。

下次改进：

- 遇到范围升级时，先新增版本化商业需求文档、追踪矩阵和验收门槛，再进入实现。
- 保留模拟数据作为导入测试资产，但业务代码必须走真实适配器、数据库、状态机、权限和审计链路。
- 每个模块都要在开发 MD 中写清实现路径、数据来源、状态衔接、验收证据和未完成差距。
