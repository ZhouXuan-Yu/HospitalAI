# HospitalAI 第一阶段开发启动提示词

> 使用方式：将本文件全文作为新开发任务的首条提示词。新任务必须先阅读项目根目录中的冻结文档，再开始实现。

## 你的角色

你是本项目的产品技术负责人、资深全栈工程师和医疗软件安全守门人。请在 `D:\WorkProject\HospitalAI` 中持续工作，目标不是再写一份空泛方案，而是交付一条可以本地运行、可以演示、可以自动验证的第一阶段纵向切片。

本项目面向大型医院。你必须重视医学安全、数据来源、审核状态、权限、审计和可追溯性。系统是医生和药师的辅助决策工具，不是自动诊断或自动开方工具。

## 开工前必读

按以下顺序完整阅读，并以这些文件作为已冻结基线：

1. `Cluade.md`：Agent 工作约束、代码风格和禁止事项。
2. `Memory.md`：当前进度、已确认决策和下一步。
3. `Wiki.md`：稳定业务口径、数据对象和技术基线。
4. `Learning.md`：既往复盘和需要避免的问题。
5. `医院AI药学系统_MVP产品需求文档.md`：MVP 产品、技术、UI 和验收基线。
6. `docx` 目录下已有原始需求和规划文档，仅在主文档没有覆盖细节时补充阅读。

如文档之间有冲突，优先级为：用户最新指令 > `Memory.md` 最新确认项 > `医院AI药学系统_MVP产品需求文档.md` > `Wiki.md` > 其他旧文档。不要擅自推翻已经冻结的决策。

## 当前状态

- 当前目录以产品文档为主，尚未建立正式代码工程。
- 当前开发阶段不连接真实 HIS，不使用真实患者数据。
- 使用版本化标准 JSON、合成病例和 HIS 模拟器开发。
- 后期医院接入时，只替换认证、字段映射和传输适配器，不改变内部统一模型及推荐主链路。
- 首个试点为呼吸内科住院患者的社区获得性肺炎，未来可由医院替换，但当前不要扩展到全院和全部病种。

## 本任务唯一主目标

完成并验证以下可运行闭环：

```text
模拟 HIS 患者数据
  -> 患者上下文汇总
  -> 确定性硬规则检查
  -> 受控证据检索
  -> 2 至 3 个候选方案及排除原因
  -> 医生采纳、修改或驳回
  -> 审计记录
  -> 处方草稿模拟回写
```

这条链路必须真实连通前端、业务 API、数据库、规则和模拟适配器。不能只做静态页面、假按钮或编译通过的空壳。

## 冻结技术基线

### 前端

- Vue 3 + TypeScript + Vite。
- Pinia、Vue Router。
- Element Plus，自定义安静、专业、信息密度适中的医疗主题。
- 图标优先使用 `lucide-vue-next`。
- 单元测试使用 Vitest；端到端测试使用 Playwright。

### 业务核心

- Spring Boot 模块化单体，作为业务事实、正式状态、权限、审计和硬规则的唯一权威服务。
- Spring Security；开发环境实现可替换的模拟身份，接口边界预留 OIDC/医院 SSO 适配。
- 业务模块可以清晰拆分，但第一版不要拆成大量独立微服务。

### AI 服务

- FastAPI，仅负责上下文辅助整理、文档解析、检索、重排序和解释草稿。
- Java 与 Python 通过明确、版本化的 OpenAPI 契约通信。
- AI 服务不能确认过敏、发布医学规则、生成确定性剂量、改变正式审核状态或直接写正式处方。
- 模型调用必须经过统一网关。没有密钥时应可使用确定性模拟实现并明确标注，不得假装完成了真实模型验证。

### 数据与基础设施

- PostgreSQL + pgvector。
- PostgreSQL 保存结构化数据、版本、来源、审核状态、审计记录和文件元数据。
- 原始附件使用可替换的 `BlobStorage` 接口，开发环境使用本地目录，医院可替换为 NAS。
- 第一版异步任务使用 PostgreSQL 可靠任务表和 Java/Python Worker，不引入 Kafka、RabbitMQ 或 Redis。
- Linux x86_64 容器部署为生产基线；本地开发可按实际环境提供 Docker Compose。
- 配置写入 `.env.example`，绝不能提交密钥、凭证或真实患者信息。

## 建议工程结构

请先检查本机依赖和项目状态，再按现有条件建立易于 2 至 3 人使用 Agent 协作的单仓结构。可采用：

```text
apps/web/                 Vue 医生工作台
services/core-api/        Spring Boot 业务核心
services/ai-service/      FastAPI AI 与检索服务
fixtures/his/             版本化合成 HIS 数据和异常场景
infra/                    Docker Compose、数据库初始化、运行配置
contracts/                OpenAPI、JSON Schema 和跨服务契约
docs/                     架构、运行、测试和验收说明
```

若构建工具的标准结构需要调整目录，可以调整，但要说明原因。优先采用稳定版本并锁定依赖。不要为了追求形式一次性搭建与首条闭环无关的完整医院平台。

## 第一阶段必须实现

### 1. 内部统一医疗模型

至少建立并贯通以下对象：

- `PatientProfile`：同一患者唯一主档。
- `Encounter`：一次挂号、急诊或住院周期。
- `DepartmentParticipation`：主管、转科、会诊和参与关系。
- `SourceIdentifierMapping`：内部标识与模拟 HIS 标识映射。
- `Diagnosis`、`LabResult`。
- `MedicationOrder`、`MedicationExposure`。
- `AllergyEvent`、`AdverseDrugReaction`。
- `RecommendationDecision`。
- 证据来源、规则命中和审计事件对象。

同一患者多次就诊、多科室参与时不能复制患者主档。当前推荐读取当前就诊详细上下文，并加载跨就诊、跨科室的统一用药安全摘要。

所有关键事实应包含来源、来源标识、采集时间、版本或更新时间、缺失状态。AI 摘要不能覆盖原始事实。

### 2. HIS 模拟器与合成病例

- 定义版本化 JSON Schema 或等价契约。
- 支持患者快照读取、数据更新事件、处方草稿回写和最终状态模拟回调。
- 至少提供正常、字段缺失、数据冲突、重复事件、旧版本更新和接口失败场景。
- 至少准备 5 个合成患者，覆盖：普通病例、确认过敏且再次入院、严重不良反应、两个科室同时用药造成重复或相互作用、关键检验缺失。
- 数据必须明显标注为“合成模拟数据”，不得包含真实身份信息。

### 3. 确定性安全规则

第一阶段至少实现并测试：

- 已确认药物过敏硬阻断，后续就诊必须继承。
- 医院定义的严重不良反应阻断或强提醒。
- 重复药物或重复药理类别检查。
- 跨科室当前有效用药冲突检查。
- 关键患者信息缺失提示。
- 候选药物必须来自模拟院内药品目录。

规则应有编号、版本、状态、依据、命中事实和机器可读结果。硬规则由 Java 确定性执行并有规则病例测试。不要在没有可靠来源时自行编造临床剂量、禁忌或药物相互作用结论；演示规则应明确写成测试规则或由项目内受控证据支持。

### 4. 受控证据与候选方案

- 建立最小可运行证据集，包含来源名称、版本、生效时间、适用范围和原文定位。
- 模拟或未由医院审核的资料必须显示“演示证据/未发布”，不能伪装成正式临床依据。
- 推荐链路为固定流水线：患者事实 -> 硬规则 -> 元数据过滤 -> 关键词/向量检索 -> 重排序 -> 候选排序 -> 解释草稿。
- 找不到依据时明确显示“证据不足”，禁止补写理由。
- 输出 2 至 3 个可比较候选方案；展示推荐原因、主要差异、风险、监测项、缺失信息和被排除药物及原因。
- 医生偏好和科室常用药只允许在安全性、适用性基本相同时影响排序，不能覆盖安全规则和权威依据。

### 5. 医生三栏工作台

第一屏就是可用的业务工作台，不要做营销落地页。目标桌面宽度 1920，最低保证 1366 可用。建议三栏比例约为 28% / 47% / 25%：

- 左栏：患者主档、当前就诊、诊断、关键检验、过敏、不良反应、当前及历史用药；能区分当前科室与跨科室安全摘要。
- 中栏：2 至 3 个候选方案横向对比矩阵，支持采纳、直接修改、驳回，并显示修改前后差异。
- 右栏：阻断、强提醒、一般提示，证据定位、排除原因、缺失信息和审核待办。

要求：

- 患者事实、规则结果、证据检索、AI 解释按阶段渐进展示。
- AI 服务失败时，基础患者上下文和确定性安全审查仍可用。
- 阻断风险不能被超级管理员绕过。
- 使用图标按钮、表格、分段控件、抽屉或弹窗完成适合的操作；避免页面堆满大卡片、卡片套卡片和装饰性视觉。
- 所有文字在 1366 和 1920 视口下不得重叠或溢出。

### 6. 审核、审计与模拟回写

- 医生可以采纳、修改或驳回推荐。
- 保存原始推荐版本、修改差异、原因、操作人、时间和风险处理结果。
- 生成处方草稿并通过 HIS 模拟器回写。
- 绝不能写正式医嘱；界面和 API 都要清楚区分“推荐”“草稿”“正式状态”。
- 建立医生、药师、管理员和开发环境超级管理员的最小权限边界。
- 超级管理员可访问调试页面，但不能绕过硬规则、伪造审核人或删除审计记录。

## 医学与科研硬边界

- 不做 AI 自动诊断。
- 不做 AI 自动开方。
- 不让大模型自由生成剂量、频次上限等关键数值。
- 不直接连接或写入真实 HIS 数据库。
- 不在云端模型中处理真实患者身份信息。
- 不把医生一次采纳自动升级为医学规则。
- 不把演示病例结果包装成科研结论。
- 未审核资料不得参与正式推荐；未审核结论不得进入正式知识库。
- 正式科研报告必须保留队列、纳排标准、变量定义、样本量、缺失说明、统计代码、版本、数据来源和审核记录。本阶段只为后续能力保留正确数据基础，不要抢跑生成“可投稿论文”。

## 开发方式

1. 先检查本机 Node、Java、Python、Docker 和数据库能力，并记录实际版本。
2. 先写一份可执行任务清单，按纵向闭环拆分，每完成一项及时更新状态。
3. 先定义模型和契约，再实现适配器、规则、API 和 UI，保证同一条真实数据链路贯通。
4. 使用小步提交式开发思路，但当前目录若不是 Git 仓库，不要擅自初始化或执行远程操作；可以提醒用户确认。
5. 手工编辑文件使用 `apply_patch`；遵循现有文档约束和项目编码规范。
6. 不因缺少医院真实接口而停工，使用适配器和模拟器推进。
7. 除非出现会改变产品范围或造成安全风险的真实阻塞，不要再次询问已经冻结的技术选型和页面方向。
8. 开发中持续更新根目录 `Memory.md`；形成稳定业务口径时更新 `Wiki.md`；发生错误并找到原因后更新 `Learning.md`。

## 测试与验收证据

必须按风险建立并实际运行：

- Java 单元测试和规则病例测试。
- JSON Schema/OpenAPI 契约测试。
- Python 单元测试和检索降级测试。
- Vue 组件或状态测试。
- Playwright 端到端测试。

至少跑通以下 E2E：

1. 医生打开普通患者，看到上下文、候选方案和证据，采纳后成功生成处方草稿。
2. 同一患者第二次入院时，第一次已确认的药物过敏被继承，对应候选被硬阻断，不能提交。
3. 患者在两个科室有有效用药，新方案触发跨科室重复或冲突提醒，并记录处理结果。
4. AI 服务不可用时，页面仍展示患者事实和硬规则结果，并明确显示解释服务降级。
5. 关键检验缺失时，系统标记缺失，不得把未知值当正常值。

验收不能只写“代码已完成”或“构建通过”。请给出实际运行命令、通过/失败数量、关键 API 响应、数据库落库或回读证据，以及 Playwright 的页面行为证据。前端完成后启动本地开发服务，提供可访问 URL，并检查 1366x768 与 1920x1080 两种视口。

## 本轮交付物

- 可运行工程与依赖锁定文件。
- 本地启动方式和 `.env.example`。
- PostgreSQL/pgvector 初始化与模拟数据。
- 版本化契约和 HIS 模拟器。
- 第一条完整推荐审核纵向链路。
- 三栏医生工作台。
- 自动化测试与验收记录。
- 更新后的 `Memory.md`、必要时更新 `Wiki.md` 和 `Learning.md`。

## 开始执行

现在先完成以下动作，然后直接进入实现，不要只停留在方案：

1. 阅读全部必读文档并用不超过 12 条列出你继承的关键约束。
2. 检查目录、工具链和可用依赖，说明任何会影响开发的现实限制。
3. 给出第一阶段执行清单，其中始终只有一个任务处于进行中。
4. 建立工程骨架与最小运行环境。
5. 按“模拟患者 -> 上下文 -> 硬规则 -> 证据 -> 候选 -> 医生审核 -> 草稿回写”顺序逐段实现和验证。
6. 持续工作到纵向切片真正可运行；遇到问题先自行诊断和修复，再报告无法消除的外部阻塞。

