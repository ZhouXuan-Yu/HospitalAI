# HospitalAI 当前进度

更新时间：2026-08-03

## 当前项目状态

项目已建立第一阶段单仓工程，包含 Vue 医生工作台、Spring Boot Core API、FastAPI AI 服务、契约、合成导入夹具、Docker Compose/Flyway 基线和自动化验证脚本。早期“仅文档规划”的状态已过期。

已有原始文档：

- `医院AI药学系统_项目需求与预算 (1)(1).docx`
- `模块一：智能处方推荐 — 技术方案详解(1).docx`

已新增规划文档：

- `集中开发方向_处方推荐_随访科研沉淀.md`
- `大型医院药学AI_双核心功能OKR与MVP边界.md`
- `医院AI药学系统_MVP产品需求文档.md`

本次新增项目记忆文件：

- `Cluade.md`
- `Memory.md`
- `Learning.md`
- `Wiki.md`

## 刚刚确认的方向

用户希望集中开发两个核心功能：

1. 处方推荐。
2. 患者长期追踪、用药反馈、科研数据沉淀和结论报告。

已确认优先级：

1. 处方推荐优先。
2. 数据沉淀和结论报告第二优先。
3. 长期随访能力必须服务于处方推荐，尤其是二次入院时继承历史过敏和不良反应。

已确认推荐依据顺序：

1. 过敏、严重不良反应、禁忌和患者当前安全信息。
2. 患者诊断、病情和检验检查指标。
3. 经过医院确认的权威指南、说明书和院内规范。
4. 患者历史用药反馈。
5. 科室常用药和医生习惯仅在同等安全、同等适用时影响排序。

已确认追踪方案：

> 用药事件链作为底座 + 一个病种的专病模板 + 通用模板兜底。

已确认首个可替换试点：

> 呼吸内科住院患者的社区获得性肺炎，后续可根据合作医院的数据和专家资源调整。

## 已确认的产品判断

第一版不做“AI 自动开方系统”，而做：

> 面向医生和药师的可审核处方推荐与患者药学随访工作台。

第一版推荐目标：

```text
用一个科室、一个病种、20 条脱敏病例，
跑通“患者上下文解析 -> 处方推荐 -> 医生审核 -> 用药反馈 -> 二次入院风险继承 -> 科研报告草稿”的完整闭环。
```

## 当前最重要的产品边界

- AI 只做推荐，不替代医生决策。
- 医生或药师必须审核。
- 过敏、禁忌、相互作用、剂量上限属于硬规则。
- 剂量计算不能交给大模型自由生成。
- 科研报告第一版是可审核草稿，不是直接可投稿论文。
- 审核后的数据和结论才能进入知识库。

## 已冻结的开发基线

1. 已确认混合交付：独立院内 Web 工作台，通过 HIS 单点登录和患者上下文进入，并预留页面嵌入及回写。
2. 已确认前端：Vue 3 + TypeScript + Vite + Pinia + Element Plus 自定义医疗主题。
3. 已确认后端：Spring Boot 业务核心 + FastAPI AI 服务，通过 OpenAPI 通信。
4. 已确认数据：PostgreSQL + pgvector，原始文档放院内对象存储。
5. 已确认模型：统一模型网关，研发环境使用脱敏数据和云端 API，医院环境使用本地 DeepSeek。
6. 已确认推荐采用固定可审计流水线，不采用通用医疗 Agent 自主决策。
7. 已确认医生三栏单屏工作台、横向方案对比矩阵、三级风险交互、决策优先患者上下文、分段多轨时间线和阶段式科研工作台。
8. 已确认按角色显示导航并采用待办驱动首页，不使用所有用户共用的功能总菜单和统计大屏。
9. 已增加调试用超级管理员，可以访问全部页面和功能权限，但不能绕过医疗硬规则或审计。
10. 已确认超级管理员在开发测试环境长期可用，医院生产环境默认禁用，调试时通过双重认证、指定内网地址、填写原因并限时开启。
11. 已确认采用 FHIR 思想的内部统一医疗模型、可替换医院适配器和开发期 HIS 模拟器。
12. 已确认“一名患者一个主档、多次就诊、多科室参与、统一用药安全视图”：当前处方跨科室读取用药和安全风险，详细病历仍按权限查看。
13. 已确认跨科室处方冲突由系统分级并创建协同任务，医生和药师处理，系统不自动取消原医嘱。
14. 已确认规则引擎采用可视化医学决策表、Java 确定性计算函数、专业审核发布和病例测试。
15. 已确认 RAG 采用元数据过滤、关键词检索、向量检索、重排序和原文定位，禁止无证据生成推荐理由。
16. 已确认资料采用“上传、自动解析、人工审核、正式发布”的受控流程，未审核资料不得参与推荐。
17. 已确定医院内网文档解析方案：电子文档优先原生提取，扫描页和复杂表格使用本地 PaddleOCR/PP-Structure，统一输出 DocumentBlock，运行时不访问外网。
18. 已确认 PostgreSQL 管理文件元数据，原始附件存放文件目录或 NAS，并通过可替换 BlobStorage 接口访问。
19. 已确认第一版使用 PostgreSQL 可靠任务表和 Java/Python Worker，不增加独立消息队列。
20. 已确认优先使用 OIDC，兼容医院其他 SSO，并通过短期一次性签名票据传递医生、患者和就诊上下文。
21. 已确认 HospitalAI 只回写处方草稿，由医生在 HIS 原生界面最终确认，禁止直接写正式医嘱。
22. 已确认采用进入页面实时快照、事件通知优先、轮询兜底和提交前版本复核的数据同步方式。
23. 已确认推荐按照患者事实、硬规则、候选证据和 AI 解释渐进展示，AI 失败时降级为基础安全审查。
24. 已确认医生在方案矩阵中直接修改并查看差异，证据可以定位到原始 PDF 页码和高亮原文。
25. 已确认第一版只做系统内待办、桌面端和 HIS 嵌入宽度，不开发移动端通知与完整移动适配。
26. 已确认 HIS 模拟器使用版本化 JSON 契约，覆盖正常、缺失、冲突、重复、更新和接口失败场景。
27. 已确认剂量计算器按药品、适应证和特殊人群拆分，证据优先级由医院配置，指南更新后只做影响分析和人工复核。
28. 已确认 Embedding 和重排序模型通过中文医学测试集评测后选择，第一版不增加 Redis。
29. 已确认科研统计使用固定 Python 分析代码，研究数据二次脱敏，知识结论按类型执行多人审核。
30. 已确认第一版以 Linux x86_64 容器部署为基线，使用 OpenTelemetry 接入监控，数据库和文件执行同批次备份及恢复演练。
31. 已确认首条开发主线为可运行纵向切片，并同时建立单元、契约、规则病例、模型回归和 Playwright 测试。
32. 已确认第一阶段通过模拟数据导入和 HIS 模拟器开发，后期接入医院时只替换认证、字段和传输适配器。

## 下一步从哪里继续

1. 第一阶段纵向切片已建立：`apps/web` Vue 三栏医生工作台、`services/core-api` Spring Boot 核心服务、`services/ai-service` FastAPI 证据服务、`fixtures/his` 合成 HIS 数据、`contracts` 契约、`infra` Compose。
2. 已跑通“模拟患者 -> 患者上下文 -> Java 硬规则 -> FastAPI 受控证据 -> 3 个候选方案 -> 医生审核 -> 审计 -> HIS 模拟处方草稿回写”。
3. 已实现 5 个合成患者场景：普通病例、二次入院过敏继承、严重 ADR、跨科室当前用药、关键检验缺失。
4. 已推进 M1 真实数据链路基座：Flyway 迁移、HIS snapshot 导入、inbound event、同步游标、工作列表 API 和契约校验已通。
5. 已推进 M2 规则与证据治理：规则生命周期、剂量计算器、EvidenceDocument/Block/Chunk、证据上传/解析/发布/撤回、FastAPI 通过 Core API 获取已发布证据已通。
6. 已推进 M3 推荐审核闭环：推荐快照状态机、医生决策、阻断不生成草稿、强提醒药师复核、跨科室协同任务、草稿幂等写入、失败重试状态、HIS callback 和数据版本过期已通。
7. 已推进 M4 患者追踪与科研资产骨架：用药事件链、反馈、不良反应信号、出院结局、研究队列、变量字典、质量检查、队列冻结、统计任务、统计运行记录、统计产物落盘、脱敏导出记录、脱敏 JSONL 产物落盘、artifact 回读、报告草稿、报告审核、知识提交、两角色知识审核发布和撤回 API 已通；FastAPI 已有固定统计端点并输出稳定哈希。
8. 本机验证使用 `h2-demo` profile，因为 Docker Desktop Linux engine 未运行，PostgreSQL 18 虽运行但无 postgres 凭据；默认工程仍保留 PostgreSQL/pgvector Compose 基线。
9. 验证记录在 `docs/validation/phase1-acceptance.md`，截图在 `docs/validation/workbench-1366.png` 和 `docs/validation/workbench-1920.png`。
10. 下一步优先补 M4 剩余项：Java 可靠 Worker 租约/调度并调用 Python 统计端点、ADR 升级联动、正式知识库 UI、引用控制、artifact 下载授权和更细角色权限。
11. 随后进入 M5：权限票据和 RBAC/ABAC、超级管理员约束硬化、OpenTelemetry、备份恢复演练、安全扫描、性能压测、最终需求追踪矩阵和验收结论。

## 2026-08-03 新增商业化交付基线

用户已要求按“完整的大型商业千万级项目”作为对标基准进行补充，并明确开发实现阶段不得依赖 Mock 或静态假数据。最新口径为：

1. 后续功能实现以真实数据链路为主：真实适配器、真实导入、真实数据库状态、真实审计和真实回写状态机。
2. 合成或模拟数据只作为“导入后的验证数据集”，用于覆盖正常、异常和边界场景，不能作为业务功能本身的 Mock 替身。
3. 当前已新增商业化需求和验收文档：`docs/00_FIRST_PRINCIPLES_COMMERCIAL_BENCHMARK.md`、`docs/01_COMMERCIAL_REQUIREMENTS_SPEC.md`、`docs/02_MODULE_IMPLEMENTATION_TRACEABILITY.md`、`docs/03_REAL_DATA_CHAIN_ARCHITECTURE.md`、`docs/04_ADVERSARIAL_REVIEW_AND_ACCEPTANCE_PLAN.md`、`docs/05_DEVELOPMENT_TASK_LIST_COMMERCIAL.md`。
4. 若旧 MVP 文档与新商业化口径冲突，按“最新用户指令优先”处理：保留 HIS 模拟器用于离线验证，但正式开发任务要落到真实连接器、导入器、契约、状态机、权限和审计路径。
5. 后续开发应按 `docs/05_DEVELOPMENT_TASK_LIST_COMMERCIAL.md` 的 M1 到 M5 推进，并用 `docs/02_MODULE_IMPLEMENTATION_TRACEABILITY.md` 做需求覆盖率和验收追踪。

## 等待医院确认的外部事项

- HIS、EMR、LIS、药品目录和处方回写的真实接口及字段。
- 医院患者主索引、就诊标识、SSO 和角色映射。
- 医院服务器、GPU、操作系统、容器平台和国产化清单。
- 医院备份、监控、审计保存和安全审查要求。
- 社区获得性肺炎专病指标、临床判定标准和审核专家。
- 可用于试点的脱敏病例、指南、药品目录及科研管理要求。
